apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "chart.fullname" . }}-server-admin-role
  labels:
  {{- include "chart.labels" . | nindent 4 }}
rules:
- apiGroups:
  - klio.cnpg.io
  resources:
  - servers
  verbs:
  - '*'
- apiGroups:
  - klio.cnpg.io
  resources:
  - servers/status
  verbs:
  - get
