apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "chart.fullname" . }}-server-viewer-role
  labels:
  {{- include "chart.labels" . | nindent 4 }}
rules:
- apiGroups:
  - klio.cnpg.io
  resources:
  - servers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - klio.cnpg.io
  resources:
  - servers/status
  verbs:
  - get
