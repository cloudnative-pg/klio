# The CNPG cluster + its PluginConfiguration, as their own buildable unit so
# ../../multi can reuse them (as cluster-a, cluster-b, ...) without
# duplicating their spec. See ../../multi/team-a for how that reuse looks.
resources:
  - cluster.yaml
  - plugin_configuration.yaml
  - cluster_pod_monitor.yaml
