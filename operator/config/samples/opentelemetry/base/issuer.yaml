# Self-signed issuer for each default-namespace server's client-cert CA
# (see ../single/server/klio_ca.yaml).
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: selfsigned-issuer
spec:
  selfSigned: {}
